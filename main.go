package main

import (
	"fmt"
)

func main() {
	// user := "three hundred thousand seven hundred and fifty four"
	// user = strings.ToLower(user)
	// slice := strings.Fields(user)
	// // fmt.Println(slice)
	// ints := translator.SliceOfInt(slice)
	// // fmt.Println(ints)
	// _, str := translator.TranslateToInt(ints)
	// fmt.Println(numb)
	// fmt.Println(str)
	fmt.Println("Words and Numbers Translator")
	fmt.Println("1. Convert Words to Numbers")
	fmt.Println("2. Convert Numbers to words")
	fmt.Print("Select An Option: ")

}
